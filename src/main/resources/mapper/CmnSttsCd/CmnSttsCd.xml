<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.teamb.btob.mapper.bizworkflow.BizWorkflowMapper">

	<!-- 견적/주문결제 상태코드 값 변경 -->
	<update id="updateStatusCd">
		
		UPDATE 
		-- 테이블
		<choose>
			<when test=""></when>
			<otherwise></otherwise>
		</choose>
		SET
		-- 변경 값
		<choose>
			<when test=""></when>
			<otherwise></otherwise>
		</choose>
		WHERE 1=1
		<choose>
			<when test="">AND est_id = #{estId}</when>
			<when test="sysId == 'ORD' or sysId == 'PAY'">AND ord_id = #{ordId}</when>
			<otherwise></otherwise>
		</choose> 	
		
	
	</update>
	
	<update id="updateStatusHist">
	
	
	</update>

</mapper>
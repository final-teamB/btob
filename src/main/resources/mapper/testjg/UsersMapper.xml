<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.teamb.btob.mapper.testjg.UsersMapper">
	
	<insert id="insertUsers" parameterType="io.github.teamb.btob.dto.testjg.Users">
        INSERT INTO TB_USERS
        (user_id, password, user_name, phone, email, company_cd, user_type, app_status, acc_status, reg_id)
        VALUES
        (#{userId}, #{password}, #{userName}, #{phone}, #{email}, #{companyCd}, #{userType}, #{appStatus}, #{accStatus}, #{regId})
    </insert>

    <select id="checkUserId" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM TB_USERS WHERE user_id = #{userId}
    </select>

    <select id="checkCompanyCd" parameterType="string" resultType="int">
        SELECT COUNT(*) FROM TB_COMPANIES WHERE company_cd = #{companyCd}
    </select>
    
</mapper>